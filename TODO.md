# TODO

- [x] Remove manual `duration` field from `data/portfolio.json` (regenerate file)
- [ ] Update `js/portfolio.js` to remove manual platform/type badges and derive them from URL
- [ ] Update `js/portfolio.js` to always render duration as `--:--` and never from JSON
- [ ] Update `js/app.js` to stop using `ds.duration` in modal subtitle
- [ ] Ensure thumbnail + title are derived automatically from URL (no API for title/thumbnail unless already in current logic)
- [ ] Verify card UI (chips, hover, animations) remains unchanged
- [ ] Provide complete updated code for modified files

